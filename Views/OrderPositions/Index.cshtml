@model IEnumerable<ShopApp.Models.OrderPosition>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.order.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.product.ProductName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.order.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.product.ProductName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrderPositionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderPositionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderPositionId })
        </td>
    </tr>
}

</table>
